JWT_USER_PASSWORD ="useronly123"  //for signing users , admin passwords would be signed by their own passwords which is different
JWT_ADMIN_PASSWORD ="adminonly123"  //for signing admins

module.exports = {
    JWT_USER_PASSWORD,
    JWT_ADMIN_PASSWORD 
}